interface Props {
    owner: string,
    repo: string,
    description: string,
    language: string,
    stars: string,
    forks: string
}

function RepoCard (props: Props) {
    return (
        <h1></h1>
    )
}

export default RepoCard